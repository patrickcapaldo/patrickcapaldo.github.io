function animate(){requestAnimationFrame(animate),globe.rotation.y+=.002,renderer.render(scene,camera)}const scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,.1,1e3),renderer=new THREE.WebGLRenderer;renderer.setSize(window.innerWidth,window.innerHeight),document.getElementById("globe-container").appendChild(renderer.domElement);const geometry=new THREE.SphereGeometry(5,32,32),texture=(new THREE.TextureLoader).load("https://threejsfundamentals.org/threejs/resources/images/earth-atmos-2048.jpg"),material=new THREE.MeshBasicMaterial({map:texture}),globe=new THREE.Mesh(geometry,material);scene.add(globe),camera.position.z=15,animate(),window.addEventListener("resize",()=>{renderer.setSize(window.innerWidth,window.innerHeight),camera.aspect=window.innerWidth/window.innerHeight,camera.updateProjectionMatrix()});